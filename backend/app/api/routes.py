"""
Diligencify Profile Builder — API Routes (placeholder)

Future routes will be added here as the application is built out:
  POST /api/profiles/generate   — trigger profile generation
  GET  /api/profiles/{id}       — retrieve a stored profile
  GET  /api/profiles/{id}/stream — SSE stream of generation events
"""

from fastapi import APIRouter

router = APIRouter()


# ── Placeholder ───────────────────────────────────────────────────────────────
# Routes will be implemented in subsequent development steps.
# The router is registered in app/main.py under the /api prefix.
